{
  "name": "supervisor-barge-coach-fns",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "twilio-run",
    "deploy": "twilio-run deploy --username $TWILIO_ACCOUNT_SID --password $TWILIO_AUTH_TOKEN --override-existing-project --production",
  
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "twilio": "^3.56",
    "@twilio/runtime-handler": "1.1.3",
    "twilio-flex-token-validator": "^1.5.6"
  },
  "devDependencies": {
    "twilio-run": "^3.2.2"
  },
  "engines": {
    "node": ">=12"
  }
}
